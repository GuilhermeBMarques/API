/*
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start API",
            "type": "shell",
            "command": "npm start",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/variedadeDog"  
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
    */
